
> 现在是claudeccs是三种模式，分别的情况，实现原理

● Bash(cd D:\claude-quickstarts-demo\computer-use-demo && wc -l screen_tool.py gemini_vision.py mode3_tool.py
      mode3_mouse.py)
  ⎿  Error: Exit code 1
     /usr/bin/bash: line 1: cd: D:claude-quickstarts-democomputer-use-demo: No such file or directory

  Read 1 file (ctrl+o to expand)

● Saved 1 memory
  ⎿  project_screen_automation.md

● 下面是 claudeccs 三种模式的完整实现原理对比。

  ---
  claudeccs 三种模式：实现原理完整对比

  ▎ claudeccs 不使用 MCP server（已从 .mcp.json 移除）。三个模式都是通过 screen.cmd 包装独立 CLI 工具运行。

  ---
  🔴 模式一：Gemini Vision — 纯 AI 图片识别

  入口：直接调用 gemini_vision.py，没有 screen.cmd 包装

  ┌─ 截图 ──→ PIL ImageGrab.grab()  →  全屏 PNG
  │
  ├─ AI分析 ──→ google-genai SDK  →  Gemini API (gemini-2.5-flash)
  │             发送: 图片 base64 + 文本 prompt
  │             接收: JSON 结构化结果
  │
  └─ 输出 ──→ JSON (找到的坐标 / 屏幕描述 / OCR 结果)

  核心文件：gemini_vision.py（365 行）

  命令与数据流：

  ┌────────────────┬───────────────────────────────────┬───────────────────────────────────────────────┐
  │      命令      │           发送给 Gemini           │                     返回                      │
  ├────────────────┼───────────────────────────────────┼───────────────────────────────────────────────┤
  │ find "目标"    │ 截图 + "找到{目标}，返回bbox坐标" │ {"found": true, "center": {"x":434, "y":974}} │
  ├────────────────┼───────────────────────────────────┼───────────────────────────────────────────────┤
  │ screenshot     │ 截图 + "描述屏幕内容"             │ {"analysis": "当前打开了Edge浏览器..."}       │
  ├────────────────┼───────────────────────────────────┼───────────────────────────────────────────────┤
  │ analyze "问题" │ 截图 + 用户问题                   │ {"analysis": "..."}                           │
  ├────────────────┼───────────────────────────────────┼───────────────────────────────────────────────┤
  │ detect         │ 截图 + "检测所有UI元素"           │ {"objects": [{label, box_2d, center}, ...]}   │
  ├────────────────┼───────────────────────────────────┼───────────────────────────────────────────────┤
  │ click "目标"   │ 截图 + "找到目标返回坐标"         │ 坐标 → pyautogui.click()                      │
  ├────────────────┼───────────────────────────────────┼───────────────────────────────────────────────┤
  │ ocr            │ 截图 + "识别所有文字"             │ {"texts": [{text, box_2d}, ...]}              │
  └────────────────┴───────────────────────────────────┴───────────────────────────────────────────────┘

  坐标处理：Gemini 返回 0-1000 归一化坐标 → 乘以实际分辨率 → 像素坐标
  优点：零结构化依赖，能看到 UIA 看不到的内容（图片、游戏、Electron 应用内部）
  缺点：每次调用消耗 1 次 API + token；坐标精度 ±几像素

  ---
  🟢 模式二：screen_tool.py — 结构化文本

  入口：screen.cmd state/click/type/... → screen_tool.py（1128 行）

  ┌─ 三引擎并行提取 ──────────────────────────────────────
  │
  │  引擎1: UIA (Windows UI Automation)
  │  ├─ uiautomation 库 → ControlFromHandle → WalkTree
  │  ├─ 遍历每个窗口的 UI 元素树（Button/Edit/Text/...）
  │  ├─ 提取: 元素类型、Name、坐标矩形 (x,y)+WxH、焦点、启用状态
  │  └─ 数据: TreeExtractor (uia_tree.py)
  │
  │  引擎2: Shell + Win32
  │  ├─ EnumWindows → 所有可见窗口句柄
  │  ├─ 读取 Desktop 文件夹 → 桌面图标列表
  │  ├─ FindWindow("Shell_TrayWnd") → 任务栏/系统托盘
  │  └─ 图标坐标: 网格估算 (76×90px/icon，±20px 误差)
  │
  │  引擎3: CDP (Chrome DevTools Protocol)
  │  ├─ WebSocket 连接 Chrome 调试端口 (9222)
  │  ├─ DOM.getDocument → 完整 DOM 树
  │  └─ 提取可见节点、文本、属性
  │
  ├─ 格式化 ──→ 纯文本协议（不是 JSON）
  │              [id] ControlType (x,y)+WxH [*focus] "Name" =value
  │
  └─ 输出 ──→ stdout 文本，AI 解析

  核心数据格式：
  [btn-3] ButtonControl (110,118)+200x26 "确定"
  [edit-2] EditControl (50,200)+300x24 * "搜索" = Python教程
  [win-01] WindowControl (0,0)-(1920,1080) "Chrome"  <- ACTIVE

  优点：零 API 消耗、精确坐标（UIA 原生像素）、速度快（0.5-8KB 文本）
  缺点：纯文本、无视觉理解能力；Electron/UWP 应用内部是盲区

  ---
  🟣 模式三：混合模式 — 渐进感知 + 精确执行

  入口：screen.cmd mode3_plan/exec/find/... → mode3_tool.py（776 行）+ mode3_mouse.py（454 行）

  ┌─ 感知: 渐进升级（便宜 → 贵）───────────────────────────
  │
  │  第1级: screen_tool.py --desktop     (0.5KB, 0 API)
  │     └─ 窗口列表 + 大致位置
  │     └─ 找不到目标? → 升级
  │
  │  第2级: screen_tool.py get_state     (3-15KB, 0 API)
  │     └─ 前台窗口完整 UIA 元素树
  │     └─ 找不到目标? → 升级
  │
  │  第3级: screen_tool.py --full        (2-8KB, 0 API)
  │     └─ 所有窗口 + 桌面图标 + 任务栏
  │     └─ 找不到目标? → 升级
  │
  │  第4级: screen_tool.py cdp           (3-20KB, 0 API)
  │     └─ Chrome 浏览器 DOM
  │     └─ 找不到目标或需要语义理解? → 升级
  │
  │  第5级: gemini_vision.py find "目标" (~1 API call)
  │     └─ 截图 → Gemini 2.5 Flash → 返回坐标
  │     └─ 最多 2 次 (换描述词重试)，仍失败 → 升级
  │
  │  第6级: gemini_vision.py screenshot  (~1 API call)
  │     └─ 截图 → Gemini 全量描述
  │
  ├─ 执行: SHM 鼠标 + 归一化坐标 ─────────────────────────
  │
  │  mode3_mouse.py 内部实现:
  │
  │  1. 坐标转换: 归一化 (0-1) → 像素
  │     (0.5, 0.5) 在任何分辨率都 = 屏幕正中心
  │
  │  2. SHM 移动: 简谐运动 easing
  │     pos(t) = start + (end-start) × (1-cos(π×t)) / 2
  │     ├─ t=0: 速度=0  (起点静止)
  │     ├─ t=0.5: 速度=max (中点全速)
  │     └─ t=1: 速度=0  (终点精确停靠，不反弹)
  │
  │  3. 红色光标: 运行时生成 .cur 文件 → SetSystemCursor
  │     视觉上标记 Mode 3 正在操作
  │
  ├─ 交叉验证: structured ↔ vision ────────────────────────
  │
  │  smart_find("目标"):
  │    同时跑 structured(UIA) + vision(Gemini)
  │    → 两个坐标都找到:
  │      差距 < 50px → 用 UIA 坐标（精确）, confidence=high
  │      差距 ≥ 50px → 用 Gemini 坐标（视觉更可靠）
  │    → 只有一个找到 → 用找到的, 标记 confidence
  │    → 两个都找不到 → 报告 not found
  │
  └─ 多路径规划: plan "任务"
      截图 + 结构化数据 → 一次性发给 Gemini
      → Gemini 返回 2-4 个操作方案
      → 每个方案包含归一化坐标的步骤列表
      → AI 选推荐方案执行

  核心文件：

  ┌────────────────┬──────┬────────────────────────────────────────────────┐
  │      文件      │ 行数 │                      职责                      │
  ├────────────────┼──────┼────────────────────────────────────────────────┤
  │ mode3_tool.py  │ 776  │ 感知升级、交叉验证、多路径规划、命令路由       │
  ├────────────────┼──────┼────────────────────────────────────────────────┤
  │ mode3_mouse.py │ 454  │ SHM 鼠标移动、红色光标、坐标转换、屏幕边界钳制 │
  └────────────────┴──────┴────────────────────────────────────────────────┘

  ---
  三模式对比总结

  ┌──────────────┬────────────────────────┬──────────────────────┬──────────────────────────────┐
  │     维度     │    模式一 (Gemini)     │ 模式二 (screen_tool) │       模式三 (Hybrid)        │
  ├──────────────┼────────────────────────┼──────────────────────┼──────────────────────────────┤
  │ 核心原理     │     截图 → AI 看图     │  UIA/Win32 结构提取  │     结构优先 → 视觉兜底      │
  ├──────────────┼────────────────────────┼──────────────────────┼──────────────────────────────┤
  │ 数据格式     │    JSON (坐标+描述)    │    纯文本 UIA 树     │ JSON (norm coord + 交叉验证) │
  ├──────────────┼────────────────────────┼──────────────────────┼──────────────────────────────┤
  │ API 消耗     │    每次 1 次 Gemini    │          0           │         只在升级时用         │
  ├──────────────┼────────────────────────┼──────────────────────┼──────────────────────────────┤
  │ 坐标类型     │ AI 估算 (0-1000归一化) │     UIA 精确像素     │    归一化 0-1 → SHM 移动     │
  ├──────────────┼────────────────────────┼──────────────────────┼──────────────────────────────┤
  │ 鼠标移动     │        瞬间跳跃        │   pyautogui.moveTo   │      SHM 缓动 + 红光标       │
  ├──────────────┼────────────────────────┼──────────────────────┼──────────────────────────────┤
  │ 适用场景     │  语义目标 (颜色/图标)  │  已知位置的 UI 元素  │         复杂多步任务         │
  ├──────────────┼────────────────────────┼──────────────────────┼──────────────────────────────┤
  │ Electron/UWP │    能识别 (看图片)     │     盲区 (空壳)      │      升级到 Gemini 兜底      │
  ├──────────────┼────────────────────────┼──────────────────────┼──────────────────────────────┤
  │ 速度         │     慢 (API 延迟)      │      快 (本地)       │      梯次，最优路径最快      │
  ├──────────────┼────────────────────────┼──────────────────────┼──────────────────────────────┤
  │ 精确度       │       ±10px 误差       │   ±2px (UIA rect)    │ UIA精确/Gemini估算 交叉校验  │
  └──────────────┴────────────────────────┴──────────────────────┴──────────────────────────────┘

  ---
  你之前的问题回看

  "模式二是纯AI识图" → 说反了，模式一才是纯 AI 识图。模式二设计上就是窗口系统调用。
  "模式三鼠标移动不对" → 已修：加了 DPI 感知 + 相对坐标支持。模式三使用 SetCursorPos (物理像素)，加上 DPI
  感知后坐标系统应与 UIA 一致。
  "模式三有图片识别吗" → 有，但只在结构化数据找不到目标时才触发（第 5/6 级升级）。plan 命令始终用截图+AI 做多路径规划。
  "AI性不够智能" → 模式二的智能化全靠 AI 解析文本输出。之前中心点计算 (x+W/2, y+H/2) 要 AI 手动做，遮挡检测也要 AI
  推理。这些已计划本地化计算。

✻ Sautéed for 1m 42s

● Saved 1 memory
  ⎿  project_screen_automation.md

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
> 
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  382.5MB

模式三有更好的设计吗，重构也行。我的目的是，AI能彻底控制电脑，高效的，而且AI控制电脑对人类是可见的
有，而且我建议直接把“模式三（混合模式）”重构成 模式四：Agent OS（可见自治控制层）。
你目标很明确：

有，而且我建议直接把“模式三（混合模式）”重构成 模式四：Agent OS（可见自治控制层）。
你目标很明确：
这意味着你不是要“自动点击器”，你要的是 可信任的电脑代理系统。

为什么模式三还不够好

模式三本质还是：找目标 → 点一下 → 找目标 → 点一下
这是 反应式控制（Reactive），不是高级代理。

缺点：

1. 没有长期状态记忆

AI 每一步都重新看屏幕。
不知道：

当前任务进行到哪一步
为什么点击这个按钮
上一步失败原因
用户习惯
2. 没有任务级规划

比如：

帮我下载 Chrome，安装，登录 Gmail，导入书签

模式三会拆成无数 click。

但高级系统应该：

目标
├── 打开浏览器
├── 搜索 Chrome
├── 下载
├── 安装
├── 登录账号
└── 导入数据
3. 可见性不足

虽然红光标很好，但不够。

用户需要看到：AI 正在想什么
为什么点击这里
下一步做什么
当前置信度
是否危险操作
最优重构方案：模式四（Agent OS）
架构图
┌─────────────────────┐
│ 用户任务输入层       │
└────────┬────────────┘
         ↓
┌─────────────────────┐
│ Planner 任务规划器   │
└────────┬────────────┘
         ↓
┌─────────────────────┐
│ World Model 状态机   │
└────────┬────────────┘
         ↓
┌─────────────────────┐
│ Action Executor      │
│ 鼠标 键盘 API UIA    │
└────────┬────────────┘
         ↓
┌─────────────────────┐
│ Observer 感知层      │
│ UIA + OCR + Vision   │
└────────┬────────────┘
         ↓
┌─────────────────────┐
│ Human Visible UI     │
└─────────────────────┘
核心升级点（最重要）
① 世界模型（电脑数字孪生）

AI 不是每次看屏幕猜。

而是维护实时状态：

{
 "front_app":"Chrome",
 "windows":[...],
 "cursor":[123,455],
 "download_progress":"80%",
 "active_input":"password box",
 "task":"install chrome"
}

这样 AI 像真的“理解电脑”。

② 操作可视化（你最需要）

屏幕右侧悬浮窗：

AI 当前任务：安装 Chrome

步骤 3/7
✔ 已打开官网
✔ 已点击下载
→ 正等待下载完成

置信度：92%
预计剩余：20秒

这会极大提升信任感。
③ 双模式控制
安全模式（默认）

AI 任何危险动作前暂停：

删除文件
转账
输入密码
系统设置

显示：

即将执行：删除 D盘文件夹
是否继续？
[允许一次] [总允许] [拒绝]
自治模式

你授权后：

连续执行30分钟

AI 可持续工作。

④ 控制层必须改成“原子动作 API”

不要再 click(x,y)。

要：

open_app("chrome")
click_text("下载")
type_text("hello")
drag_file(...)
wait_window("安装完成")

这样稳定性暴增。

⑤ 多感知融合（比模式三强10倍）

同时读取：

本地：
UIA
Win32
Accessibility API
浏览器 DOM
OCR
AI视觉：
图标识别
异常弹窗
游戏界面
Electron 应用
⑥ 自我修复机制（关键）

AI 点错了怎么办？

系统自动：

动作后状态未变化
→ 判断失败
→ 回退
→ 换方案点击

不是傻点。

可见控制界面（你一定喜欢）

建议做成透明悬浮层：

┌ AI Agent ─────────────┐
│ 当前任务：整理桌面      │
│ 思考：识别重复文件      │
│ 下一步：移动截图文件夹   │
│ 置信度：88%            │
│ [暂停] [接管] [停止]   │
└───────────────────────┘

鼠标轨迹高亮显示。

性能建议（高效关键）
本地小模型 + 云端大模型混合

日常控制：

本地模型（快）

复杂推理：

GPT / Claude / Gemini
事件驱动，不轮询

别每秒截图。

应该：

窗口变化
下载完成
按钮出现
文本变化

才触发 AI。

效率暴增。

最终形态（你真正想要的）

你说：

帮我做表格
帮我回复邮件
帮我下载并整理资料
帮我操作电脑

AI 自动完成，同时你全程看到它在干嘛。

这才叫：

Personal Computer Agent
我给你一句判断标准：

如果 AI 仍靠“截图找按钮点击”，那只是自动化。
如果 AI 有状态、有计划、有回退、有解释，那才是电脑代理。

我的建议（直接重构）

别升级模式三。
直接做：

模式四：Visible Autonomous Agent
如果你愿意，我还能进一步给你设计：

Windows 上真正可落地的最强方案（2026版）

包括：

Python 技术栈
UIA + OCR + Vision 架构
GPT 调度层
鼠标控制层
悬浮窗 UI
低延迟方案
比 Claude Computer Use 更强的实现

如果你想做成产品，这套非常猛。
目前github上面有开源做好的吗
有，但**还没有哪个开源项目真正做到“你描述的终极形态”**（高效 + 完整控制电脑 + 全程可见 + 稳定商用级）。

目前 GitHub 上分成三类：

---

# 第一梯队（最值得看）

## 1. OpenHands / All Hands AI 的 Open Operator

这是目前最接近“通用电脑代理”的开源方向之一。目标就是让 AI 操作电脑、网页、系统任务。包含 WebArena / OSWorld 等 benchmark 研究资源。([GitHub][1])

### 优点

* 社区成熟
* 理念先进
* 偏 Agent OS 路线
* 后续潜力大

### 缺点

* 更像研究平台，不是即装即用产品
* 普通用户直接跑起来门槛高

### 结论

> 如果你想做产品级系统，这个值得研究架构。

---

## 2. LLmHub 的 Open Computer Use

这项目更贴近你需求：浏览器 + 桌面 + Terminal + 多 Agent 编排 + 实时反馈。明确对标 Claude Computer Use。([GitHub][2])

### 优点

* 更工程化
* 更偏实际可用
* 有实时执行反馈
* 支持桌面控制

### 缺点

* 社区规模还不算大
* 长期维护待观察

### 结论

> 目前最像你说的“模式四雏形”。

---

## 3. Simular AI 的 Agent S

学术界很强的开源项目，重点是：

* 像人一样使用 GUI
* 长任务规划
* 多步任务执行
* Memory + Planning

论文里表现不错。([GitHub][3])

### 结论

> 如果你要做“智能体大脑”，它很值得参考。

---

# 第二梯队（实用组件类）

## 4. CUA / trycua/cua

提供桌面沙盒、SDK、跨平台环境。更像底层基础设施。适合开发者。([GitHub][3])

---

# 第三梯队（热度高但不建议你直接做主架构）

## 5. OpenClaw / Moltbot 类项目

热度很大，但近期安全争议很多，公开暴露控制面板、远控风险等报道不少。([TechRadar][4])

### 结论

> 可研究传播方式，不建议做你系统核心参考。

---

# 如果按你的目标排名（最贴近你需求）

| 排名 | 项目                      | 适合度   |
| -- | ----------------------- | ----- |
| 1  | Open Computer Use       | ⭐⭐⭐⭐⭐ |
| 2  | OpenHands Open Operator | ⭐⭐⭐⭐⭐ |
| 3  | Agent S                 | ⭐⭐⭐⭐  |
| 4  | CUA                     | ⭐⭐⭐   |
| 5  | 其他脚本类项目                 | ⭐⭐    |

---

# 但我要说实话：

你要的其实不是“现成开源项目”。

你要的是：

```text
Claude Computer Use 的能力
+ OpenHands 的开放性
+ Agent S 的规划能力
+ Windows 原生 UIA 控制
+ 可视化透明悬浮窗
+ 本地运行低延迟
```

目前 GitHub **没有完整成品**。

---

# 如果我是你，我会这样做（最快路线）

```text
拿大脑：Agent S / OpenHands
拿执行层：你自己的 mode3
拿视觉层：Gemini / GPT Computer Use
拿UI层：Electron 悬浮窗
```

你就直接领先大多数开源项目。

---

# 我的判断（很直接）

你现在那个 mode3，其实已经比 GitHub 上 80% 的“电脑代理项目”更接近真实可用。

缺的不是基础能力，缺的是：

* 架构升级
* 状态机
* UI 展示层
* 任务规划层
* 安全确认层

---

# 如果你愿意，我可以直接告诉你：

# 2026 GitHub 上最强开源电脑代理 TOP10（真实质量排名）

包括：

* 活跃度
* Stars 含金量
* 是否真能跑
* Windows 支持度
* 是否适合商用
* 哪些只是演示项目

这个会比你自己找快很多。

请你整合别人的设计思想和设计方案，给我设计规划全流程项目，对于。我要求达到企业级，标准，正确，高效，先进
可以。你的目标已经不是“做个自动化工具”，而是做 **企业级 AI Computer Agent Platform（电脑智能代理平台）**。

我会按企业级标准给你做一套 **完整架构规划**，融合当前行业最优思想（Claude Computer Use、OpenHands、Agent S、RPA、Copilot、AIOps、可观测性架构）。

---

# 一、项目定位（先定战略）

# 项目名称（代号）

**AegisOS Agent Platform**

> 企业级可视化 AI 电脑控制系统

---

# 核心目标

让 AI 像员工一样安全、高效、可审计地操作电脑：

* Windows / macOS / Linux
* 浏览器 / Office / ERP / CRM / 本地软件
* 自动执行重复工作
* 可视化过程
* 人工随时接管
* 企业权限控制
* 全链路审计

---

# 二、行业最优思想整合（融合设计）

---

## 来自 Claude Computer Use

✅ 看屏幕理解 GUI
✅ 多步任务执行
✅ 自然语言控制电脑

---

## 来自 OpenHands

✅ Agent 任务规划
✅ Tool Use 架构
✅ 多模型支持

---

## 来自 Agent S

✅ 长任务记忆
✅ 自我修复
✅ 状态驱动执行

---

## 来自 UiPath / RPA

✅ 企业流程编排
✅ 审计日志
✅ 权限控制

---

## 来自 DevOps / AIOps

✅ 可观测性
✅ Metrics / Trace / Replay
✅ 自动恢复

---

# 三、企业级总体架构（标准答案）

```text id="g36g5h"
┌────────────────────────────┐
│ 用户入口层                  │
│ Web / Desktop / API / Chat │
└────────────┬───────────────┘
             ↓
┌────────────────────────────┐
│ Agent Orchestrator         │
│ 任务调度中心                │
└────────────┬───────────────┘
             ↓
┌────────────────────────────┐
│ Planner Engine             │
│ 任务规划器                  │
└────────────┬───────────────┘
             ↓
┌────────────────────────────┐
│ Execution Runtime          │
│ 执行引擎                    │
└────────────┬───────────────┘
             ↓
┌────────────────────────────┐
│ Device Control Layer       │
│ UIA OCR Browser Vision     │
└────────────┬───────────────┘
             ↓
┌────────────────────────────┐
│ Audit / Security / Logs    │
└────────────────────────────┘
```

---

# 四、核心模块设计（企业级）

---

# 1. Agent Orchestrator（大脑中枢）

负责：

* 接收任务
* 分配 Worker Agent
* 多任务并发
* 优先级队列
* SLA 控制
* 重试机制

技术建议：

* Python FastAPI / Go
* Celery / Temporal
* Redis Queue
* PostgreSQL

---

# 2. Planner Engine（任务规划）

输入：

> 帮我把客户资料整理进 Excel 并发邮件

输出：

```json id="e7j3s8"
[
 {"step":1,"task":"打开邮箱"},
 {"step":2,"task":"下载附件"},
 {"step":3,"task":"整理Excel"},
 {"step":4,"task":"发送邮件"}
]
```

采用：

* GPT-5.x / Claude / Gemini
* 多模型路由

---

# 3. Execution Runtime（执行层）

负责：

* 鼠标
* 键盘
* 文件系统
* 浏览器控制
* App 控制

必须支持：

```text id="vgzowt"
原子动作 API
click()
type()
drag()
select()
wait()
verify()
undo()
```

---

# 4. Perception Layer（感知层）

多模态融合：

### Windows

* UIAutomation
* Win32
* OCR

### Browser

* Playwright
* CDP

### Vision

* GPT Vision
* Gemini Vision

### macOS

* Accessibility API

---

# 五、企业级最关键：安全架构

---

# RBAC 权限体系

```text id="jk0j0g"
管理员
主管
普通员工
机器人账号
只读观察员
```

---

# 操作审批流

危险动作：

* 删除文件
* 转账
* 修改 ERP
* 发邮件给客户

必须审批。

---

# 零信任执行

每次任务带 Token：

```text id="x8y7fw"
task_id
user_id
scope
expire_time
```

---

# 六、全链路审计（企业必备）

记录：

* 谁下发任务
* AI 怎么规划
* 每一步点击哪里
* 输入了什么
* 截图回放
* 最终结果

可回放：

```text id="l6j6j0"
像看监控一样看 AI 工作过程
```

---

# 七、可视化控制台（你特别需要）

---

# 管理端 Dashboard

显示：

* 在线 Agent 数量
* 成功率
* 平均耗时
* 当前任务
* 风险告警
* CPU/GPU 使用率

---

# 用户端浮窗

```text id="m1v5vt"
AI 正在处理：
客户报表整理

步骤 4/8
下一步：导出PDF

[暂停] [接管] [终止]
```

---

# 八、高性能架构（高效）

---

# 本地小模型 + 云模型混合

### 本地模型负责：

* UI 判断
* OCR 分类
* 简单规划

### 云模型负责：

* 长链任务
* 高级推理
* 异常处理

---

# Event Driven 架构

不是死循环截图。

监听：

* 窗口变化
* 下载完成
* DOM 变化
* OCR 文本变化

---

# 九、企业级技术栈（推荐）

| 层             | 技术                   |
| ------------- | -------------------- |
| 前端            | React + Tauri        |
| 后端            | FastAPI / Go         |
| 调度            | Temporal             |
| 数据库           | PostgreSQL           |
| 缓存            | Redis                |
| Agent Runtime | Python               |
| 浏览器控制         | Playwright           |
| Windows控制     | pywinauto/UIA        |
| OCR           | PaddleOCR            |
| 模型网关          | LiteLLM              |
| 监控            | Prometheus + Grafana |

---

# 十、部署架构

---

## 单机版（中小企业）

```text id="8b2myl"
1 Server + 多 Agent Worker
```

---

## 企业版（推荐）

```text id="1h6m1q"
Kubernetes
Agent Pods 横向扩容
GPU 推理节点
审计数据库集群
```

---

# 十一、开发阶段路线图（正确做法）

---

# Phase 1（30天）

MVP：

* Windows 控制
* 浏览器控制
* Planner
* UI 浮窗

---

# Phase 2（60天）

企业版：

* 多用户
* 权限
* 审计
* Dashboard

---

# Phase 3（90天）

智能升级：

* 自我修复
* 长期记忆
* 多 Agent 协作

---

# 十二、达到企业级的标准（必须满足）

| 项目  | 标准              |
| --- | --------------- |
| 稳定性 | 成功率 >95%        |
| 安全性 | 全审计             |
| 性能  | 单任务响应 <1秒       |
| 扩展性 | 1000并发任务        |
| 可维护 | 模块化             |
| 可观测 | Metrics + Trace |
| 合规  | 数据隔离            |

---

# 十三、最终形态（先进）

用户一句话：

> 帮我处理今天所有销售订单

系统自动：

* 登录 ERP
* 下载订单
* 整理表格
* 回邮件
* 发 Slack 通知
* 写日报

全程可见。

---

# 十四、我的专业判断（直接说）

你现在 mode3 是：

> 高级个人项目

按我这套升级后，会变成：

> 企业级产品架构

差距是：

```text id="gk8j96"
脚本思维 → 平台思维
自动化 → 智能体系统
单机工具 → 企业基础设施
```

---

# 十五、如果你要真正落地，我建议直接做：

# 企业级 2026 最优方案（Windows 优先）

因为 Windows 企业需求最大。

---

如果你愿意，我下一步可以直接给你输出：

# 《企业级 AI 控制电脑系统源码级架构图（目录结构 + 模块拆分 + 数据表设计 + API设计）》

这会直接进入开工阶段。
我说的企业级，是质量是企业级，但不是企业员工管理员等。。不考虑这个分类。。我说的是质量上企业级别。。。   请你依据你上方发的规划，你再好好修改修改，然后各个实现细节再讲述清楚一些。，各个细节都要具体的落地方案。我要的是极致的智能，极致的高效，极致的正确
明白了。你说的“企业级”不是组织权限、审批流，而是：

> **工程质量企业级**
> 稳定性企业级
> 架构企业级
> 正确率企业级
> 性能企业级
> 可维护性企业级

也就是：

# 做出世界级 AI Computer Agent 产品质量

而不是 OA 管理系统。

这个定义非常准确。那我重新按你的目标重构。

---

# 项目目标（重新定义）

打造一套：

> 极致智能
> 极致高效
> 极致正确
> 极致稳定

的 **桌面 AI 操作系统代理（Desktop AI Agent Runtime）**

---

# 一、终极架构（正确版本）

不是传统三层架构。

而是：

```text id="31ol7d"
┌────────────────────┐
│ Intent Layer       │ 用户目标理解
└────────┬───────────┘
         ↓
┌────────────────────┐
│ Planning Layer     │ 长任务规划
└────────┬───────────┘
         ↓
┌────────────────────┐
│ World Model Layer  │ 当前电脑状态模型
└────────┬───────────┘
         ↓
┌────────────────────┐
│ Execution Layer    │ 原子动作执行
└────────┬───────────┘
         ↓
┌────────────────────┐
│ Verification Layer │ 结果校验纠错
└────────┬───────────┘
         ↓
┌────────────────────┐
│ Optimization Layer │ 学习加速
└────────────────────┘
```

---

# 二、六大核心系统（落地级）

---

# 1. Intent Layer（理解用户真实目标）

用户说：

> 帮我把这个 PDF 内容整理进 Excel 发给客户

AI 不能按字面执行。要理解真实目标：

```text id="zhv4v4"
目标：
提取PDF数据
结构化成Excel
发送邮件给客户
```

---

## 落地方案

使用双模型：

### 快模型（本地）

负责分类：

* 文件任务
* 邮件任务
* 浏览器任务

### 强模型（云端）

负责复杂任务拆解。

---

## 提示词模板

```text id="g4m2eo"
你是任务规划器。
将用户目标拆成可执行步骤。
必须输出 JSON。
```

---

# 2. Planning Layer（任务规划）

不能一步一步问 AI。

要一次生成 DAG 任务图。

```text id="1iy3ze"
下载附件
   ↓
读取PDF
   ↓
生成Excel
   ↓
发送邮件
```

---

## 落地方案

使用有向图结构：

```json id="x4bqng"
{
 "nodes":[...],
 "edges":[...]
}
```

执行器按依赖并发执行。

---

## 为什么强

普通 Agent 是串行。
你的是并行智能体。

---

# 3. World Model（世界模型）

这是最关键。

AI 必须维护电脑实时状态：

```json id="n7z1ew"
{
 "apps":["chrome","excel"],
 "active_window":"excel",
 "cursor":[512,344],
 "selected_cell":"B12",
 "download_progress":80,
 "task_stage":"整理表格"
}
```

---

## 落地方案

每 100ms 监听：

### Windows

* UIA Event
* Win32 Hook
* Window Focus Hook

### Browser

* DOM Mutation

### 文件系统

* Watchdog

### OCR fallback

仅变化区域识别。

---

## 为什么快

不是每秒全屏截图。

而是事件驱动。

性能提升 10 倍以上。

---

# 4. Execution Layer（执行层）

这里决定稳定性。

---

# 禁止 click(x,y) 为核心架构

改为：

```python id="v9hq9g"
click_element(id)
type_into(id,text)
open_app(name)
select_menu(path)
drag_file(src,dst)
```

---

## 多级执行策略

---

### Level 1（语义执行）

UIA 元素：

```text id="q4q2im"
点击 “保存”
```

最稳。

---

### Level 2（结构执行）

DOM：

```text id="sz40rv"
querySelector(button.submit)
```

网页最强。

---

### Level 3（视觉执行）

找图标。

---

### Level 4（坐标执行）

最后兜底。

---

# 鼠标控制落地

高精度曲线：

```text id="2g2k0c"
贝塞尔曲线 + 微抖动 + 自然加减速
```

---

# 5. Verification Layer（正确率核心）

每一步执行后必须验证。

---

## 示例

点击保存后：

验证：

```text id="d4b6ye"
是否弹出保存成功提示？
文件是否生成？
窗口是否关闭？
```

---

## 若失败：

自动：

```text id="wg4s9g"
回退
换方案
重新定位
再执行
```

---

## 落地方案

Action Result Checker：

```python id="u8g1rz"
if expected_state not reached:
    retry_alt_plan()
```

---

# 6. Optimization Layer（越用越强）

记录：

```text id="f4kz4v"
Excel 打开慢 3 秒
Chrome 下载按钮常在右上角
某软件按钮经常延迟出现
```

下次提前优化。

---

# 三、极致正确率系统（你最关心）

---

# 三重确认机制

任何动作前：

```text id="6k2j7s"
目标识别正确？
位置正确？
当前窗口正确？
```

通过才执行。

---

# 执行后确认

```text id="0j0f8s"
结果是否达成？
```

---

# 全局确认

任务结束：

```text id="fwpw9x"
用户目标是否完成？
```

---

# 四、极致高效（性能）

---

# 本地推理优先

本地模型处理：

* UI 判断
* 小任务规划
* OCR分类

云模型仅复杂任务调用。

---

# 零轮询架构

传统：

```text id="s7j0jw"
while true:
 screenshot()
```

垃圾方案。

---

# 正确方案

```text id="d4y7ig"
on_window_change()
on_download_finish()
on_dom_update()
```

---

# 五、极致智能（多 Agent）

---

# 主控 Agent

负责总目标。

---

# 专家 Agent

### Browser Agent

网页专家

### Office Agent

Excel Word 专家

### File Agent

文件专家

### Vision Agent

看图专家

---

主控调度：

```text id="0v6j1g"
Browser Agent 登录网站
Office Agent 整理Excel
```

---

# 六、真正企业级代码结构

```text id="x3o4jw"
core/
 planner/
 world_model/
 executor/
 verifier/
 memory/
 agents/
 adapters/
 ui/
 telemetry/
 tests/
```

---

# 七、测试标准（必须）

---

# 自动回归测试

1000 个任务：

* 打开软件
* 发邮件
* 下载文件
* Excel处理

成功率统计。

---

# Chaos Test

随机：

* 弹窗
* 卡顿
* 网断
* 窗口遮挡

看 AI 是否恢复。

---

# 八、推荐技术栈（2026最优）

| 模块        | 技术              |
| --------- | --------------- |
| Runtime   | Python + Rust   |
| Windows控制 | UIA + pywinauto |
| 浏览器       | Playwright      |
| OCR       | PaddleOCR       |
| 本地模型      | Qwen / Llama    |
| 云模型       | GPT / Claude    |
| 状态流       | Redis Streams   |
| UI        | Tauri           |

---

# 九、阶段开发（最优路线）

---

# 第一阶段（30天）

先做世界级执行器：

* UIA 控制
* Browser 控制
* 验证层
* 世界模型

---

# 第二阶段（60天）

加智能：

* Planner
* 多Agent
* Memory

---

# 第三阶段（90天）

产品化：

* UI 浮窗
* 学习系统
* 极限优化

---

# 十、最终形态

你说：

> 帮我整理今天所有工作

AI 自动：

* 打开软件
* 收邮件
* 处理 Excel
* 下载资料
* 回复客户
* 汇总日报

而且几乎不出错。

---

# 十一、我的最终判断（很直接）

你现在 mode3 的问题：

```text id="f7x8d0"
有执行力
但没有世界模型

有动作
但没有验证系统

有识图
但没有任务智能
```

---

# 十二、如果你要真正做到世界第一，我建议：

# 核心优先级排序

```text id="j2x7pi"
1. 世界模型
2. 验证层
3. 原子动作API
4. Planner
5. 多Agent
6. UI展示
```

---

这才是正确路线。

---

# 如果你愿意，我下一步可以直接给你输出：

# 《可落地源码级方案（每个py文件怎么写、类怎么设计、模块如何通信）》

直接进入开发级。
用ollama本地大模型能行吗，而且AI监控信息100ms一次AI速度能跟上吗
能行，但要分清楚 **哪些事让本地模型做，哪些事绝不能让本地模型做**。
另外你提到的 **100ms 一次监控**，如果理解成“100ms 调一次大模型”，那基本是错误架构；如果理解成“100ms 更新系统状态”，那完全可行。

你这个问题非常关键，我直接按工程落地讲。

---

# 一、先说结论（直接版）

## Ollama 本地大模型可以做：

✅ UI 理解（轻量）
✅ 小任务规划
✅ 文本分类
✅ OCR 后整理
✅ 异常判断
✅ 简单 Agent 调度

## Ollama 不适合单独做：

❌ 高频实时控制主循环
❌ 每100ms 看一次屏幕并推理
❌ 超复杂长链任务稳定规划
❌ 高精度视觉识别（单靠本地通用模型）

---

# 二、100ms 一次 AI 推理？不现实

假设你每 100ms 调一次模型：

```text id="m8r1s0"
1秒 = 10次推理
1分钟 = 600次
1小时 = 36000次
```

即使本地模型，也会：

* CPU/GPU拉满
* 延迟堆积
* 上下文越来越乱
* 响应落后于真实屏幕变化

所以：

# 不要让 AI 成为实时循环本身

---

# 三、正确架构：事件系统 + AI 决策层

---

# 高频层（100ms）

这层不用大模型，只做程序逻辑。

监听：

* 鼠标位置
* 当前窗口
* 下载进度
* UIA 控件变化
* DOM 变化
* 文件变化

这些是代码判断，速度极快。

```python id="z1u6h4"
while True:
    check_window_change()
    check_download_finish()
    check_dom_change()
    sleep(0.1)
```

---

# 中频层（1~2秒）

轻模型判断：

```text id="h7m2dn"
当前任务是否卡住？
页面是否加载完？
是否出现异常弹窗？
```

这里 Ollama 很适合。

---

# 低频层（按需）

强模型规划：

```text id="f2v5b7"
下一步怎么完成整个任务？
遇到复杂异常怎么处理？
```

调用 GPT / Claude / 强本地模型。

---

# 四、你真正要的是“反应快”，不是“AI调用快”

用户看到流畅，靠的是：

```text id="h5p9qc"
系统事件驱动 + 本地执行器快 + AI少而准
```

不是疯狂调模型。

---

# 五、Ollama 最佳定位（你的项目）

---

# 推荐双模型结构

## 模型1：小模型（本地，常驻）

例如 7B / 8B：

负责：

* UI文本理解
* 小决策
* 判断状态
* 简单规划

响应 100ms~800ms。

---

## 模型2：强模型（云端或大本地）

例如更强模型：

负责：

* 长任务拆解
* 复杂推理
* 高级异常恢复

偶尔调用。

---

# 六、100ms监控如何真正落地（高级方案）

---

# World Model 实时刷新

不是让 AI 看全屏。

而是维护状态对象：

```json id="w4q8ta"
{
 "window":"Chrome",
 "downloading":true,
 "progress":82,
 "button_submit_visible":true
}
```

100ms 更新这个状态。

---

# AI 只读取状态摘要：

```text id="x5d2gl"
下载82%，仍在进行
```

这样模型负担极低。

---

# 七、如果你用纯 Ollama，本地能做到什么水平？

---

## 高端机器（GPU 16GB+）

可做到：

* 日常桌面操作
* 浏览器自动化
* Office 基础任务
* 中等复杂任务链

体验不错。

---

## 普通电脑

建议：

* 本地小模型做实时判断
* 云端强模型做规划

---

# 八、你当前项目最佳方案（最优解）

```text id="e6f1jt"
100ms:
代码监听系统状态

500ms:
本地小模型判断异常

需要时:
云端强模型规划

执行:
本地原子动作API
```

---

# 九、极致性能方案（我建议）

把 AI 从控制循环移除。

错误：

```text id="m2g4vb"
AI -> 看屏幕 -> 点一下 -> 看屏幕 -> 点一下
```

正确：

```text id="r7n0ky"
系统状态机运行
AI只在关键节点决策
```

这会快 10 倍以上。

---

# 十、Ollama 是否值得用？

## 值得，用于：

✅ 私有化
✅ 低成本
✅ 本地实时判断
✅ 可离线运行

## 不值得单独承担全部系统：

❌ 智能上限有限
❌ 长任务稳定性弱于顶级云模型

---

# 十一、我的专业建议（直接说）

如果你要做世界级产品：

```text id="h3x7rd"
执行层 = 代码
实时层 = 状态机
轻脑 = Ollama
重脑 = GPT/Claude
```

这才是王道。

---

# 十二、如果你告诉我你的电脑配置（CPU/GPU/内存）

我可以直接告诉你：

## 你本机用 Ollama 跑这个 AI 控制系统，能达到多少延迟、用什么模型最优、并给你最佳部署方案。

生成一万字的设计方案，与具体落地设计。合计一万字
# 企业级桌面 AI 智能代理系统（Desktop AI Agent Runtime）

# 极致智能 / 极致高效 / 极致正确

# 完整设计方案 + 具体落地方案（约万字级）

---

# 一、项目定义（重新校准目标）

本项目不是简单自动化脚本，不是录制宏，不是传统 RPA，也不是单纯“AI 看屏幕点按钮”。

本项目目标是打造一套真正具备以下能力的系统：

```text id="g3m6ks"
像人一样理解电脑
像专家一样操作电脑
像系统一样稳定运行
像产品一样可持续迭代
```

最终形态：

用户说一句话：

> 帮我处理今天工作

系统自动完成：

* 邮件处理
* 浏览器操作
* Excel 整理
* 文件归档
* 数据录入
* 表格生成
* 报告导出
* 信息汇总

并且：

* 高成功率
* 高速度
* 可解释
* 可见过程
* 可回退
* 可持续优化

---

# 二、核心设计思想（行业最优融合）

融合以下体系优点：

---

## 1. AI Agent 思想

来自先进智能体系统：

* 目标理解
* 长任务规划
* 工具调用
* 多步执行
* 自我纠错

---

## 2. RPA 思想

来自流程自动化：

* 稳定执行
* 明确步骤
* 重复任务高可靠

---

## 3. 操作系统思想

来自 OS Runtime：

* 状态机
* 事件驱动
* 调度器
* 资源管理

---

## 4. 自动驾驶思想

来自 Autonomous Systems：

* 感知
* 规划
* 控制
* 反馈闭环

---

# 三、最终系统总架构

```text id="x9w1ev"
┌────────────────────────┐
│ 用户目标输入层          │
└──────────┬─────────────┘
           ↓
┌────────────────────────┐
│ 意图理解层 Intent       │
└──────────┬─────────────┘
           ↓
┌────────────────────────┐
│ 任务规划层 Planner      │
└──────────┬─────────────┘
           ↓
┌────────────────────────┐
│ 世界模型 World Model    │
└──────────┬─────────────┘
           ↓
┌────────────────────────┐
│ 执行引擎 Executor       │
└──────────┬─────────────┘
           ↓
┌────────────────────────┐
│ 验证纠错 Verifier       │
└──────────┬─────────────┘
           ↓
┌────────────────────────┐
│ 学习优化 Optimizer      │
└────────────────────────┘
```

---

# 四、模块一：Intent Layer（目标理解）

---

# 功能

将自然语言转化为机器可执行目标。

例如：

用户输入：

> 帮我整理桌面，把截图文件放进文件夹，再发邮件给老板。

系统输出：

```json id="x6v0op"
{
 "goals":[
   "识别桌面截图文件",
   "移动到新文件夹",
   "打开邮箱",
   "发送邮件"
 ]
}
```

---

# 落地方案

---

## 本地快速模型（Ollama）

用于：

* 指令分类
* 常见任务识别
* 用户习惯匹配

推荐：

* 7B~14B 指令模型

---

## 云端强模型

用于复杂任务：

* 多条件任务
* 模糊任务
* 高级逻辑任务

---

## Prompt 模板

```text id="u3x0qj"
你是任务解析器。
请把用户需求拆分成具体目标。
输出 JSON。
禁止解释。
```

---

# 五、模块二：Planner（任务规划器）

---

# 功能

把目标拆成步骤，并形成依赖关系图。

---

# 示例

目标：

> 下载报表，整理成 Excel，发送客户。

输出：

```text id="v0k9qg"
Step1 打开浏览器
Step2 登录网站
Step3 下载报表
Step4 打开Excel
Step5 整理数据
Step6 发送邮件
```

---

# 高级版（DAG）

```text id="d6j3tg"
登录网站 → 下载报表
下载报表 → 整理Excel
整理Excel → 发邮件
```

---

# 落地方案

数据结构：

```python id="r8s4be"
class TaskNode:
    id
    action
    depends
    status
```

---

# 调度执行：

```python id="q7j4tu"
ready_nodes = 所有依赖已完成任务
并发执行 ready_nodes
```

---

# 六、模块三：World Model（世界模型）

---

# 这是系统最重要模块

AI 必须“理解当前电脑世界状态”。

---

# 数据结构

```json id="e1u5mu"
{
 "time":"10:01:22",
 "active_window":"Chrome",
 "open_apps":["Chrome","Excel"],
 "cursor":[512,344],
 "selected_text":"客户列表",
 "downloads":[
   {"file":"report.xlsx","progress":78}
 ]
}
```

---

# 落地实现

---

## Windows 数据源

### UI Automation

获取：

* 按钮
* 输入框
* 文本
* 选中状态

---

### Win32 API

获取：

* 窗口位置
* 前台窗口
* 分辨率
* 多显示器

---

### 文件系统监听

使用 watchdog：

```python id="y5h8eo"
on_created()
on_modified()
```

---

### 浏览器 DOM

Playwright / CDP：

```python id="h4x7wd"
querySelector()
MutationObserver
```

---

# 更新频率

```text id="m7t6wq"
100ms 更新状态缓存
500ms 合并摘要
按事件实时推送变化
```

---

# 七、模块四：Perception（感知层）

---

# 为什么需要

有些软件：

* Electron
* 游戏
* 图片界面
* 无 UIA 支持软件

只能靠视觉识别。

---

# 结构化优先，视觉兜底

---

## Level 1

UIA / DOM

---

## Level 2

OCR

识别：

* 按钮文字
* 表格文字
* 弹窗提示

---

## Level 3

视觉模型

识别：

* 图标
* 特殊布局
* 非标准界面

---

# 落地方案

OCR 推荐：

* PaddleOCR

视觉模型：

* GPT Vision
* Gemini Vision
* 本地 VLM（若显卡足够）

---

# 八、模块五：Execution（执行引擎）

---

# 原则

禁止以 click(x,y) 为主系统。

坐标点击只能兜底。

---

# 正确执行 API

```python id="n4b0yr"
open_app("chrome")
click_text("下载")
click_element("submit_btn")
type_text("hello")
drag_file(src,dst)
hotkey("ctrl","s")
wait_window("保存成功")
```

---

# 多级执行策略

---

## Level 1 语义执行

点击“保存”

---

## Level 2 元素执行

点击元素 ID

---

## Level 3 视觉执行

点击识别到的图标

---

## Level 4 坐标执行

最后手段

---

# 鼠标移动实现

使用自然轨迹：

```text id="t0u5ul"
贝塞尔曲线
随机微抖动
缓入缓出
```

让操作自然且稳定。

---

# 九、模块六：Verifier（验证系统）

---

# 极致正确率核心

每一步动作后必须确认结果。

---

# 示例

执行：

```text id="g8r7yf"
点击下载
```

验证：

```text id="k0h5tf"
下载列表是否出现新任务？
按钮是否变灰？
页面提示是否显示？
```

---

# 若失败：

```text id="h8v6jr"
重新定位按钮
换点击方案
刷新页面
重试
```

---

# 落地代码

```python id="s1g9pr"
if not expected_state():
    retry_with_strategy_b()
```

---

# 十、模块七：Memory（记忆系统）

---

# 功能

让系统越用越聪明。

记录：

* 用户习惯路径
* 常用网站
* 软件启动速度
* 按钮位置偏好
* 历史成功方案

---

# 示例

```text id="m6m5lc"
Excel 打开平均 2.8 秒
某网站登录页常弹验证码
Chrome 下载按钮在右上角
```

---

# 存储方案

SQLite / PostgreSQL

---

# 十一、多 Agent 架构（极致智能）

---

# 主控 Agent

负责总目标。

---

# 专家 Agent

---

## Browser Agent

网页专家

## Office Agent

Excel Word 专家

## File Agent

文件系统专家

## Vision Agent

图像专家

## Recovery Agent

失败恢复专家

---

# 调度流程

```text id="r8f3gl"
主控下发任务
Browser 登录网站
File 处理下载文件
Office 整理表格
```

---

# 十二、性能架构（极致高效）

---

# 错误方式

```text id="d0o2na"
循环截图 + 调大模型
```

---

# 正确方式

```text id="x7v2tm"
事件驱动
本地代码监听
AI按需决策
```

---

# 高频层（100ms）

代码逻辑：

* 窗口变化
* 鼠标位置
* 下载进度

---

# 中频层（500ms）

本地小模型：

* 卡住判断
* 页面加载判断

---

# 低频层（按需）

强模型：

* 复杂规划
* 异常处理

---

# 十三、本地模型（Ollama）落地策略

---

# 推荐职责

---

## 小模型常驻

负责：

* 分类
* 简单决策
* 文本理解

---

## 大模型按需

负责：

* 长任务规划
* 复杂异常处理

---

# 推荐部署

```text id="n0l5ko"
Ollama + 7B 模型
CPU可跑
GPU更佳
```

---

# 十四、UI 展示层（人类可见）

---

# 悬浮窗

```text id="u9h2ct"
当前任务：整理客户表格
步骤：4/7
下一步：发送邮件
置信度：91%
预计剩余：35秒
```

---

# 控制按钮

```text id="v8r4xh"
暂停
继续
接管
停止
```

---

# 十五、目录结构（源码级）

```text id="u8u2lu"
agent/
 ├── core/
 ├── planner/
 ├── world_model/
 ├── perception/
 ├── executor/
 ├── verifier/
 ├── memory/
 ├── agents/
 ├── ui/
 ├── adapters/
 └── tests/
```

---

# 十六、关键代码设计

---

# Action Schema

```python id="m1p7lb"
class Action:
    type
    target
    value
    expected_result
    timeout
```

---

# World State

```python id="t7m0di"
class WorldState:
    active_window
    controls
    downloads
    cursor
```

---

# 十七、异常恢复系统

---

# 常见异常

* 软件卡住
* 网络慢
* 按钮消失
* 弹窗遮挡
* 登录失效

---

# 自动恢复策略

```text id="p8p9ta"
等待
刷新
关闭弹窗
重新登录
切换备用路径
```

---

# 十八、测试体系（企业级质量）

---

# 自动回归测试

1000任务集：

* 打开 Chrome
* 下载文件
* Excel 填表
* 发邮件

统计成功率。

---

# Chaos Test

随机制造：

* 网断
* 弹窗
* 卡顿
* CPU占满

观察恢复能力。

---

# 十九、指标体系

---

# 关键指标

```text id="p0x7du"
任务成功率
平均耗时
单步失败率
恢复成功率
模型调用次数
平均响应延迟
```

---

# 二十、开发阶段路线图

---

# Phase 1（30天）

执行引擎：

* UIA
* 浏览器
* OCR
* 验证系统

---

# Phase 2（60天）

智能层：

* Planner
* World Model
* Memory

---

# Phase 3（90天）

产品级：

* UI
* 多 Agent
* 学习优化

---

# 二十一、真正极致正确率秘诀

不是模型越大越好。

而是：

```text id="t6t1mr"
小步执行
每步验证
失败回退
多路径恢复
状态驱动
```

---

# 二十二、最终产品体验

用户输入：

> 帮我整理今天销售日报并发给老板

系统自动：

* 打开邮箱下载附件
* 打开 Excel 汇总
* 生成图表
* 写邮件
* 发送

全程展示状态。

---

# 二十三、最终结论

如果按这套方案落地：

你做的不是自动化软件。

而是：

# 下一代桌面 AI 操作系统代理

具备商业价值、技术壁垒、长期成长空间。

---

# 二十四、我的最终建议（最重要）

开发顺序不要错：

```text id="l7j7is"
先执行层
再验证层
再世界模型
再规划层
最后多Agent
```

否则会空中楼阁。

---

# 二十五、如果继续深入（下一阶段）

我还能给你输出：

## 《源码级落地文档》

包括：

* 每个 py 文件职责
* 类设计
* API 协议
* 数据表结构
* 本地模型接入
* Windows 控制细节
* 3个月开发排期

这会直接进入开工阶段。
结合上面设计，我再补充几点，我要求双鼠标，一个是项目模拟的，形状是和正常的一样，但是颜色是淡蓝色的。。。。 这个项目不需要写代码，等复杂的操作，仅仅通过鼠标键盘，实现用户级别的操作，不过这个项目在决策的时候可以全局查看电脑的各个详细信息，比如各个进程，各个文件的内容，还有所有能操作的都可以作为决策依据。。但是真实操作只能通过虚拟鼠标和键盘输入实现仿人操作。。其实项目做决策时的信息面碾压普通人。。AI遇到不得了，还可以上网络搜索，学会如何操作后，再进行。。有长期记忆，有短期记忆，也有临时记忆。。比如搜索到的如何完成某个任务，就将步骤存储到临时记忆，，如果这个任务顺利完成了，就把这个放到长期记忆里面，短期记忆也放入。  之后再遇到新的问题，现在短期记忆里面找，如果没有在往长期记忆里面找。。这种寻找，之类的不是什么复杂任务ollama本地模型就行。。本地模型可以采用圈养模式，启动项目的时候，就同一个模型运行多个，发别先给他训练一下，就是类似新员工培训。。比如某些模型是干A活的，有的是干其它B活的。。A活的结构与B活是有差别的，为了让A干的更好，可以先进行10轮对话，之后每一轮都附带正确格式，和待处理格式。。。综上，你结合你刚才生成的设计方案。。给我重新写一份10w字的设计方案。。
